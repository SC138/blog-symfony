
{% extends 'admin/base.html.twig' %}

{% block main %}

    {% for category in categories %}
        <article>
            <h2 style="color: {{ category.color }};">{{ category.title }}</h2>
            <a href="{{ path('admin_show_category', {'id': category.id}) }}">J'aime le cliquetis des claviers wollah</a>
            <a href="{{ path('admin_delete_show_category', {'id': category.id}) }}">Supprimer la catégorie</a>
        </article>
    {% endfor %}

{% endblock %}